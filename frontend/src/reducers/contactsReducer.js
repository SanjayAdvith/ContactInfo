//import { CREATE_CONTACT_REQUEST } from '../constants/contactsConstant'

export const contactsCreateReducer = (state = {}, action) => {
    switch (action.state) {
        default:
            return state
    }
}